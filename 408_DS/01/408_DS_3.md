---
title: 习题
titleTemplate: 408数据结构
description: 数据结构基本概念习题
layout: doc
navbar: true
sidebar: true
aside: true
outline: deep
lastUpdated: true
prev: false
next: false
---

# 习题

## 单项选择题

<QuizChoice 
  question="01．可以用 _______ 定义一个完整的数据结构。"
  :options="[
    '数据元素',
    '数据对象', 
    '数据关系',
    '抽象数据类型'
  ]"
  :correctAnswer="3"
  explanation="抽象数据类型（ADT）是一个数学模型以及定义在该模型上的一组操作。它包含了数据对象、数据关系和基本操作集，能够完整地定义一个数据结构。数据元素、数据对象和数据关系只是数据结构的组成部分，而抽象数据类型是对整个数据结构的完整定义。"
/>

<QuizChoice 
  question="02．下列四种数据结构中， _______ 是非线性数据结构。"
  :options="[
    '树',
    '字符串', 
    '队列',
    '栈'
  ]"
  :correctAnswer="0"
  explanation="树是非线性数据结构，因为树中的数据元素之间存在一对多的关系，不是简单的线性关系。而字符串、队列和栈都是线性数据结构，它们的数据元素之间存在一对一的线性关系。"
/>

<QuizChoice 
  question="03．下列选项中，属于逻辑结构的是 _______ 。"
  :options="[
    '顺序表',
    '哈希表', 
    '有序表',
    '单链表'
  ]"
  :correctAnswer="2"
  explanation="有序表是逻辑结构，它描述的是数据元素之间的逻辑关系（有序性）。而顺序表、哈希表和单链表都是存储结构，它们描述的是数据在计算机中的存储方式。"
/>

<QuizChoice 
  question="04．下列关于数据结构的说法中，正确的是 _______ 。"
  :options="[
    '数据的逻辑结构独立于其存储结构',
    '数据的存储结构独立于其逻辑结构', 
    '数据的逻辑结构唯一决定其存储结构',
    '数据结构仅由其逻辑结构和存储结构决定'
  ]"
  :correctAnswer="0"
  explanation="数据的逻辑结构独立于其存储结构。逻辑结构是从逻辑关系上描述数据，而存储结构是数据在计算机中的存储方式。同一逻辑结构可以有多种存储结构实现，它们是相互独立的概念。"
/>

<QuizChoice 
  question="05．在存储数据时，通常不仅要存储各数据元素的值，而且要存储 _______ 。"
  :options="[
    '数据的操作方法',
    '数据元素的类型', 
    '数据元素之间的关系',
    '数据的存取方法'
  ]"
  :correctAnswer="2"
  explanation="在存储数据时，除了存储数据元素的值，还需要存储数据元素之间的关系信息，这样才能完整地表示数据结构。例如，在链表中需要存储指针来表示元素间的链接关系，在树中需要存储父子关系等。"
/>

## 综合应用题

01．对于两种不同的数据结构，逻辑结构或物理结构一定不相同吗？

::: details 【提示】
应该注意到，数据的运算也是数据结构的一个重要方面。

对于两种不同的数据结构，它们的逻辑结构和物理结构完全有可能相同。比如二叉树和二叉排序树，二叉排序树可以采用二叉树的逻辑表示和存储方式，前者通常用于表示层次关系，而后者通常用于排序和查找。虽然它们的运算都有建立树、插入结点、删除结点和查找结点等功能，但对于二叉树和二叉排序树，这些运算的定义是不同的，以查找结点为例，二叉树的平均时间复杂度为 $O(n)$ ，而二叉排序树的平均时间复杂度为 $O\left(\log _{2} n\right)$ 。
:::

<br>

02．试举一例，说明对相同的逻辑结构，同一种运算在不同的存储方式下实现时，其运算效率不同。

::: details 【提示】
线性表既可以用顺序存储方式实现，又可以用链式存储方式实现。

在顺序存储方式下，在线性表中插入和删除元素，平均要移动近一半的元素，时间复杂度为 $O(n)$ ；而在链式存储方式下，插入和删除的时间复杂度都是 $O(1)$ 。
:::